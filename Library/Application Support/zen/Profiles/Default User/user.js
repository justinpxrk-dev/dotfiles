// Managed by chezmoi — Zen copies user.js values into prefs.js at every
// startup, which keeps userChrome.css/userContent.css loading enabled with
// no manual about:config step on a fresh profile.
user_pref("toolkit.legacyUserProfileCustomizations.stylesheets", true);
