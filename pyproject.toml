[project]
name = "chezmoi"
version = "0.1.0"
description = "Dotfiles, managed by chezmoi."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.ruff]
exclude = ["**/lib"]

[dependency-groups]
dev = [
    "materialyoucolor>=3.0.2",
    "pyright>=1.1.409",
    "sympy>=1.14.0",
]
