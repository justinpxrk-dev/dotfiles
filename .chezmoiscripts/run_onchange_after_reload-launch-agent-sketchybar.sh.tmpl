#!/usr/bin/env bash
# Reload the default sketchybar instance (top bar) when its plist changes (and bootstrap it on first install).
# {{ include "Library/LaunchAgents/me.justinpxrk.sketchybar.plist.tmpl" | sha256sum }}

set -euo pipefail

"{{ .chezmoi.homeDir }}/.scripts/macos/reload-launch-agent.sh" me.justinpxrk.sketchybar
